[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -m "not slow"
markers =
    slow: hits live external services (Groq/yfinance/etc.) - excluded by default; run explicitly with `pytest -m slow`
